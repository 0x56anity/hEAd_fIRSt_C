//#include "encrypt.h"

void encrypt2(char *message)
{
	while (*message) {
//		puts(message);
		*message = *message ^ 31;
		message++; 
	}
}
void encrypt3(char *message)
{
	while (*message) {
//		puts(message);
		*message = *message ^ 31;
		message++; 
	}
}
void encrypt(char *message)
{
	while (*message) {
//		puts(message);
		*message = *message ^ 31;
		message++; 
	}
}
void encrypt4(char *message)
{
	while (*message) {
//		puts(message);
		*message = *message ^ 31;
		message++; 
	}
}
